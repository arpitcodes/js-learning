/**
 * let's start with the math module in js
 * lets unleash the power of maths
 */

const sideA = 3;
const sideB = 4;

// we would be calculating the hypotenuse of a triangle using pythogorus formula

const sideC = Math.sqrt(sideA**2 + sideB**2);

// console.log(`The hypotenuse is: ${sideC}`);


const randomNumber = Math.floor(Math.random()*10)+1;
console.log(`Random Number: ${randomNumber}`)



const decimalNumber = 3.4567
console.log(`Round of number is: ${Math.round(decimalNumber)}`);

// The formula (Math.random() * (max - min + 1)) + min generates a random integer within a specified range.


